import 'package:flutter/material.dart';

const Color cWhite = Color(0xffffffff);
const Color cGrey = Colors.grey;
const Color cBlue = Color(0xFF0A406D);
const Color cRed = Color(0xFFF44336);
const Color cWhiteShadow = Color(0xEBDDDADA);

const String aLoading = 'assets/animations/loading.json';

// TextStyle Type
TextStyle kLargeTitleStyle(Color color) {
  return TextStyle(
    color: color,
    fontSize: 22,
    fontWeight: FontWeight.w600,
  );
}
