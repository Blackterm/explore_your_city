import 'package:flutter/material.dart';

const Color kWhite = Color(0xffffffff);
const Color kGrey = Colors.grey;
const Color kBlue = Color(0xFF0A406D);

// TextStyle Type
TextStyle kLargeTitleStyle(Color color) {
  return TextStyle(
    color: color,
    fontSize: 22,
    fontWeight: FontWeight.w600,
  );
}
